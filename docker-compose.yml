version: '3.7'

services:
  db:
    image: postgres
    container_name: servicedb
    env_file:
      - ./envs/postgres.env
    networks:
      - dbnet
    
  bakend:
    image: app/appmarks
    container_name: bakend
    ports:
      - 4567:4567
    env_file:
      - ./envs/app.env    
    depends_on:
      - db
    networks:
      - dbnet

  

networks:
  dbnet:
    driver: bridge